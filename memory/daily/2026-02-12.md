# 2026-02-12

## Gateway Guardian v2 (21:20 CST)

David reported being offline again — Venice DIEM credits ran out, all providers entered cooldown, and the agent was brain-dead. Had to manually run `sudo curl -fsSL https://clawd.bot/install.sh | bash` to get back online.

### Root Cause (same as Feb 11)
The Gateway Guardian v1 only checked HTTP health (dashboard alive = OK). But the failure mode is that the gateway process is alive and HTTP 200 while ALL model providers are in cooldown. Gateway alive ≠ agent functional.

### Gateway Guardian v2 — What Changed
1. **Inference probe (NEW):** After HTTP check passes, runs `openclaw agent --message "Reply with only the word ALIVE"` to verify actual inference works
2. **Separate failure counters:** HTTP failures (2 threshold) and inference failures (3 threshold, ~6 min) tracked independently
3. **Nuclear restart option (NEW):** Step 4 escalation runs `curl -fsSL https://clawd.bot/install.sh | bash` — the exact command David had to run manually
4. **Signal notification (NEW):** Attempts to notify David via signal-cli before going nuclear
5. **macOS timeout fix:** Uses perl-based `run_with_timeout` since macOS lacks `timeout(1)`
6. **Gateway token auto-detection:** Reads OPENCLAW_GATEWAY_TOKEN from launchd plist for authenticated probes

### Restart Escalation Chain
1. `openclaw gateway restart` (graceful — resets cooldown state)
2. Hard kill (`kill -9`) + launchd KeepAlive auto-restart
3. `launchctl kickstart -k` (force restart)
4. **NUCLEAR:** `curl -fsSL https://clawd.bot/install.sh | bash` (full reinstall)

Each step verifies inference works (not just HTTP) before declaring success.

### Files Changed
- `skills/everclaw/scripts/gateway-guardian.sh` — complete rewrite (v1 → v2)
- `~/.openclaw/workspace/scripts/gateway-guardian.sh` — deployed copy
- `~/Library/LaunchAgents/ai.openclaw.guardian.plist` — added OPENCLAW_GATEWAY_TOKEN env var

### Key Insight
The real fix for "all providers in cooldown" is simply restarting the gateway. OpenClaw's cooldown state is in-memory — a restart clears it. The nuclear reinstall (`curl install.sh | bash`) is a guaranteed clean restart because it reinstalls the binary and restarts the launchd service.

### Inference Probe Evolution
- First attempt used `openclaw agent --message` but that requires `--to` or `--session-id` — doesn't work headless
- Second attempt tried `openclaw health --json` but that only checks process health, not model availability
- **Final approach:** Direct HTTP probes to provider endpoints (Venice `/api/v1/models`, Morpheus `/health`, mor-gateway `/api/v1/models`). If ANY provider responds, inference is available. Simple, fast, no auth needed.
- macOS lacks `timeout` command — not needed for the final approach (curl has `--max-time`)

## Everclaw v0.9.0 Published (21:47 CST)

David requested v0.9.0 release. Tagged and pushed to GitHub.

### Changes
- **SKILL.md:** v0.8.0 → v0.9.0, Gateway Guardian section rewritten for v2 (inference probes, 4-stage escalation, state files, config table)
- **README.md:** Guardian feature table updated (HTTP+inference probes, nuclear restart, Signal notification)
- **Website (docs/index.html + website/index.html):** v0.9.0 badge, "What's New in v0.9" hero section, updated Guardian card, v0.8 moved to history row, 6-column version history
- **Tag:** v0.9.0 on `profbernardoj/everclaw`

### Guardian v2 Tested Successfully
- Manual `--verbose` test: `OK: Fully healthy (PID=, HTTP=200, venice=ok)`
- launchd auto-run confirmed (runs=1, exit code 0)
- Provider probes work: Venice API returns 200, Morpheus proxy returns `{"status":"ok"}` with 2 active sessions (kimi-k2.5 + glm-4.7-flash expiring ~Feb 18)

### Venice DIEM Credits
- Venice uses "DIEM" as internal credit/pricing unit (maps 1:1 to USD pricing)
- When DIEM credits exhaust, Venice returns billing errors → cooldown cascade
- Credits appear to reset daily (David was offline during the day, back in evening)

## Multi-Key Auth Rotation Setup (22:01 CST)

David revealed he has 6 Venice API keys. Previously only one was configured, which is why the backup key was never used — OpenClaw's auth system was never told about it.

### Root Cause
OpenClaw supports multiple auth profiles per provider with automatic rotation (built in). But we only had `venice:default` with one key. The second key existed but wasn't registered as an auth profile.

### Fix
Added all 6 keys as `venice:key1` through `venice:key6` in auth-profiles.json, with explicit `auth.order.venice` in openclaw.json to try them in DIEM-balance order:

| Profile | DIEM Balance | Key Suffix |
|---------|-------------|------------|
| venice:key1 | 98 | ...uFbwtvu4QaUV |
| venice:key2 | 50 | ...sfZ7GG6FJ4_p |
| venice:key3 | 40 | ...PkVPLcch5PTe |
| venice:key4 | 26 | ...N_W2vRAMdsua |
| venice:key5 | 20 | ...CI5ozHZRHWSc |
| venice:key6 | 12 | ...ZsVJc7pfn2yD |

### Key Learning: auth.order is Critical
Without `auth.order`, OpenClaw uses round-robin (oldest-used first), which doesn't respect credit balances. Explicit order ensures highest-credit key is always tried first.

### Key Learning: Billing Disable vs Cooldown
- Billing errors → **profile disabled** (5-hour backoff, doubles per failure, caps 24h)
- Rate limit errors → **cooldown** (1→5→25→60 min exponential)
- Both only affect the specific profile, not the provider. Other profiles for same provider remain active.

### Session Stickiness
OpenClaw pins a chosen auth profile per session (cache-friendly). Won't flip-flop mid-conversation. Rotates only on billing failure, session reset, or compaction.

## Everclaw v0.9.1 Published (22:16 CST)

### Changes
- **SKILL.md:** Section 13 rewritten with multi-key setup guide, auth.order config, rotation mechanics, DIEM credit cost table, complete failover docs
- **README:** Multi-key rotation added to core features table
- **Website:** v0.9.1 badge, dual-column What's New, new Multi-Key feature card
- **Tag:** v0.9.1 on profbernardoj/everclaw

## ClawHub Name Collision + Safe Installer (22:26 CST)

### Problem
User Barry installed Everclaw v0.7 via git. His agent Janet ran `clawhub update everclaw` to upgrade to v0.8. ClawHub fetched a COMPLETELY DIFFERENT product — "Everclaw Vault" (encrypted cloud memory, slug `everclaw`, owner `kn732f1vmcycvykq1gp4meydas80dshj`, v0.3.3) — and overwrote Barry's entire skills/everclaw/ directory.

Runtime infrastructure survived (~/morpheus/, proxy, guardian live outside skill dir). But all docs, scripts, references, security tools in the skill folder were nuked.

### Root Cause
1. Someone else published "Everclaw Vault" under the `everclaw` slug on ClawHub
2. Our skill is distributed via `git clone` but has no ClawHub marker
3. `clawhub update` matches by directory name → fetches from registry → overwrites

### Fixes (v0.9.2)
1. **install-everclaw.sh** — safe installer with collision detection, git-based updates, version check
2. **CLAWHUB_WARNING.md** — prominent warning file with recovery steps
3. **SKILL.md** — collision warning banner, new install section with safe installer
4. **README.md** — curl|bash installer as primary, collision warnings throughout
5. **Website** — updated install steps, What's New covers collision protection

### ClawHub Publishing (pending)
- Our profbernardoj GitHub account is only 3 days old, ClawHub requires 7 days
- Plan: David publishes from his account under slug `everclaw-inference`
- Steps prepared for David to execute manually

### Tag: v0.9.2 pushed to profbernardoj/everclaw

## Everclaw Published on ClawHub (23:19 CST)
- **Live at:** https://clawhub.ai/DavidAJohnston/everclaw-inference
- Published by David from his GitHub account
- Updated all docs to make ClawHub the primary install method

## SmartAgent Repo Created (23:53 CST)

### Repo: SmartAgentProtocol/smartagent
- **URL:** https://github.com/SmartAgentProtocol/smartagent
- **Domain:** smartagent.org (David owns it, DNS not configured yet)
- **Logo:** David provided a futuristic AI face image — needs to be added to assets/

### What was built:
1. **install.sh** — One-command installer: Node.js check/install → OpenClaw → Everclaw → bootstrap-gateway → workspace config → WebChat launch
2. **config/** — Pre-configured workspace files (SOUL.md, AGENTS.md, BOOTSTRAP.md, TOOLS.md, USER.md, IDENTITY.md, HEARTBEAT.md) tuned for non-technical users
3. **CI** — GitHub Actions: ShellCheck + syntax validation on macOS and Linux
4. **PR template + issue templates** — Bug report and feature request
5. **Branch protection** — 1 review required, dismiss stale reviews, strict status checks
6. **ARCHITECTURE.md** — Full technical design doc (from previous session)
7. **README.md** — Install guide, architecture diagram, upgrade path, commands

### Key Design Decisions:
- **Installer-first, not fork** — SmartAgent = OpenClaw + Everclaw + defaults. No fork maintenance.
- **Primary model: mor-gateway/kimi-k2.5** — free, no API key needed
- **WebChat as default** — no phone setup friction
- **Progressive upgrade path:** free gateway → own key → Venice → MOR staking

### DNS Settings Needed (David):
- Point smartagent.org to GitHub Pages (will set up when docs/index.html is ready)

### Landing Page (00:06 CST)
- PR #1: Add landing page for smartagent.org (feat/landing-page branch)
- Hero with curl|bash install, feature grid, how-it-works, upgrade path, architecture diagram
- Fixed 3 ShellCheck warnings (unused variables: OPENCLAW_MIN_VERSION, SMARTAGENT_REPO, ARCH)
- docs/CNAME set to smartagent.org

### DNS + GitHub Pages (00:28-00:46 CST)
- Registrar (sav.com) only allows nameserver changes, no direct A records
- David set up Cloudflare (free) as DNS provider
- Updated nameservers from sav.com defaults to Cloudflare
- Added 4 GitHub Pages A records (185.199.108-111.153) in Cloudflare (DNS only, no proxy)
- Enabled GitHub Pages: main branch, /docs folder
- HTTPS enforcement pending DNS propagation

### smartagent.org Live (09:12-09:25 CST)
- David figured out direct DNS at sav.com registrar (skipping Cloudflare)
- Site confirmed live: https://smartagent.org serving GitHub Pages with HTTPS
- PR #1 merged (landing page + CNAME)

### Language Reframe: Own Your Inference (09:56-11:28 CST)
- David requested: stop saying "free" — say "own your inference" instead of "rent from big tech"
- MOR staking = inference forever (tokens staked, returned, recycled)
- **Everclaw:** Pushed directly to main — "free" → "owned"/"community-powered"/"open beta"
- **SmartAgent:** PR #2 (feat/own-your-inference) created via sub-agent, all 8 files updated, CI passed, David merged
- Key substitutions: "Free to Start" → "Own Your AI", "FREE" badges → "OPEN ACCESS"/"OWN FOREVER"

### Remove Llama 3.3 References (11:56 CST)
- David: "Llama 3.3 is fairly old now, focus on GLM and Kimi"
- Removed Llama 3.3 from Everclaw README, docs, and website
- Replaced with GLM-4.7 Flash in model tables
- Featured models now: Kimi K2.5, GLM-4, Qwen3

### GLM-4 → GLM-4.7 Flash (12:25 CST)
- David: "Instead of GLM-4 let's use GLM 4.7-FLASH as it's the one available on Morpheus"
- Updated all Everclaw references: README, docs/index.html, website/index.html
- Pushed to profbernardoj/everclaw main

### Morpheus + BasedAI Installer Variants (16:01 CST)
- David requested: design SmartAgent install variants for Morpheus (mor.org) and BasedAI (basedai.co)
- Asked David clarifying questions:
  1. Morpheus version — branded for Morpheus community, same stack, mor.org links?
  2. BasedAI version — how should it differ? Venice as primary? Enterprise branding? Different personality?
  3. Separate repos or variants in same repo?
- **Awaiting David's response** before building

### New Models Added to Config (22:48 CST)
- GLM-5, GLM-5:web, MiniMax M2.5, MiniMax M2.5:web added to mor-gateway provider
- Config patched, gateway restarted
- Everclaw README, SKILL.md, docs, website updated and pushed
- SmartAgent PR #3 created (feat/new-models) for landing page + README updates
- Also removed llama-3.3-70b from mor-gateway config (replaced)

### Clawstin Event Notes (21:36 CST)
- David shared full transcript of the Clawstin Austin meetup
- Three presentations: Agent Matchmaking, R2/SWATS (bot gave its own talk), Voice-First Agents (Greg/Ghost Security)
- Drafted Morpheus community notes at drafts/2026-02-12-clawstin-morpheus-notes.md
- Sent full notes to David on Signal (ProtonMail email pending — needs 2FA)
- David's vision: "LinkedIn for AI agents" — trust-network-based collaboration

### Family Briefings Set Up (22:47-23:10 CST)
- Morning briefing: 6:30 AM CT weekdays (cron: 73d4d4e7)
- Pickup briefing: 2:40 PM CT weekdays (cron: 65d88f12)
- Family reminders file: memory/family-reminders.md
- Standing: hair, water bottles, Sofia Tuesday cooking (2 snacks + apron + closed-toe shoes)
- Tomorrow specifics: Savannah game (dining room), Sofia takes Sydney's snack box, Sydney packs own lunch

### Morpheus + BasedAI Installer Variants (16:01-16:59 CST)
- David's answers received:
  - Morpheus version: Morpheus branding, Morpheus-only models, no Venice. Bootstrap → app.mor.org → MOR staking
  - BasedAI version: Agnostic (Morpheus + Venice), commercial/enterprise focus, premium models (M2.5, GLM-5), own API gateway eventually
  - Separate repos: Morpheus in SmartAgentProtocol org, BasedAI in dedicated private org
  - BasedAI GitHub Enterprise: https://github.com/enterprises/basedai/ (invite sent to profbernardoj but not received yet)
- Awaiting: repo naming, Morpheus domain, API bootstrap approach answers

### Overnight Research (23:34 CST)
David assigned 13 items to research overnight:
- 5 New Skills: memory setup, Kubera finance, agent control dashboard, skill directory, security check
- 8 Tasks: Peter's inference video response, Morpheus capital contracts, ETH staking yield model, Coinbase agentic wallets, hire agents on Base, model routing comparison, SaaS replacement, OpenClaw managed service
- Two sub-agents spawned to research in parallel
- Output: drafts/2026-02-13-skills-research.md and drafts/2026-02-13-tasks-research.md

### Multi-Key Rotation Validated (20:17 CST)
- David intentionally exhausted key1 credits to test rotation
- Seamlessly fell over to key2 — David didn't even notice
- Confirms the auth.order rotation works as designed

### SXSW Reminder Set (20:54 CST)
- David spoke about Everclaw at Clawstin event
- Reminder set for Feb 19 10 AM: sign up to speak at next event around SXSW

### Still TODO:
- Add SmartAgent logo to assets/ (David to upload)
- Test install.sh on clean macOS and Linux machines
- Build Morpheus and BasedAI installer variants
- Accept BasedAI GitHub Enterprise invite (not received yet)
- Set up Gmail (gog auth) for programmatic email
- Email Clawstin notes from ProtonMail (needs 2FA)
- Merge SmartAgent PR #3 (new models)
- **Live at:** https://clawhub.ai/DavidAJohnston/everclaw-inference
- **Slug:** `everclaw-inference` (NOT `everclaw` — that's the squatter)
- **Published by:** [REDACTED] from his GitHub account
- **Install:** `clawhub install everclaw-inference`
- **Update:** `clawhub update everclaw-inference`
- David downloaded from GitHub releases (v0.9.2 zip), removed .DS_Store and non-text files (LICENSE, docs/CNAME, templates/*.plist), drag-and-dropped to ClawHub
- Updated all docs (SKILL.md, README, CLAWHUB_WARNING.md, website) to make ClawHub the primary install method
- Added .gitignore to prevent .DS_Store from entering repo again
- Cancelled the Feb 16 cron job for publishing (no longer needed)
